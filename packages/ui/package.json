{
  "name": "@flashroute/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm exec tsc -p tsconfig.json --noEmit",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('ui test coverage comes from apps/web')\"",
    "build": "pnpm exec tsc -p tsconfig.json --noEmit",
    "dev": "node -e \"console.log('ui package has no standalone dev server')\""
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "lucide-react": "^0.511.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
